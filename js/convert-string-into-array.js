// Parameters: string only? yes. will the string always have a value? yes. punctuation? no
// Returns: return or print? return. as an array of words or letters? words.
// Examples: stringToArray("can we do it yes we can") = ["can", "we", "do", "it", "yes", "we", "can"]
// PseudoCode:
// .split(" ")
//return 

function stringToArray(string) {
    return string.split(" ")
}