// P: will the values always be valid? yes. will the values be ints or floats? both
// R: return or console.log()? return
// E: getVolumeOfCuboid(5, 6.6, 8) => 264
// P: multiply arguments
//    return answer

class Kata {
    static getVolumeOfCuboid(length, width, height) {
        return length*width*height
    }
}

